
lumos DemoProgram;
vars
int i , j;
bool check;

!! - This is the first comment
!! - This is the second comment

task max(int a , int b) : int {
    if (a >= b) {
        a = a + 1;
        return a;
    } else {
        return b;
    }
}

main {
    vars
    int x, i;
    string text;
    x = 10.5;
    text = "hello";

    print(max(x, i));
    
    while (true) {
        i = i + 1;
    }
    
}

nox;
